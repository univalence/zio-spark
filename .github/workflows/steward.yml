name: Univalence Scala Steward

on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch:

jobs:
  steward:
    runs-on: ubuntu-latest
    name: DIY Steward
    steps:
      - uses: actions/checkout@v3

      - name: Generate token
        id: generate-token
        uses: tibdex/github-app-token@v1
        with:
          app_id: 207992
          private_key: ${{ secrets.SCALA_STEWARD_PRIVATE_KEY }}

      - name: Launch Scala Steward
        uses: scala-steward-org/scala-steward-action@v2
        with:
          github-token: ${{ steps.generate-token.outputs.token }}
          author-email: 207992+univalence-scala-steward[bot]@users.noreply.github.com
          author-name: univalence-scala-steward[bot]
          github-app-id: 207992
          github-app-key: ${{ secrets.SCALA_STEWARD_PRIVATE_KEY }}